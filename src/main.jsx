import React from "react";
import ReactDOM from "react-dom/client";
import PageMind from "./PageMind.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PageMind />
  </React.StrictMode>
);
